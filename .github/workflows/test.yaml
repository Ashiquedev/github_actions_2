name: test

on:
    push:
    workflow_dispatch:  
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, World!"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: success step
        run: |
          echo "I succeeded"
          exit 0
      - name: Say goodbye
        run: echo "Goodbye!"
