name: 02 - Workflow Events
on: 
  push:
  workflow_dispatch:
  pull_request:
  schedule:
    - cron: "* * * * *"


jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo "${{ github.event_name }}"